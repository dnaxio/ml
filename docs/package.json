{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "clean": "rm -rf .data .nuxt .output dist node_modules && bun i",
    "dev": "nr dev:prepare && nuxi dev --host",
    "generate": "nuxt generate",
    "dev:prepare": "nuxt prepare",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@baybreezy/docd": "latest"
  },
  "devDependencies": {
    "@antfu/ni": "latest",
    "@types/node": "latest",
    "nuxt": "^4.4.5",
    "typescript": "^6.0.3",
    "vue": "latest"
  }
}
